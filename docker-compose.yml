version: "3"
services:
  backend:
    container_name: backend
    build: .
    image: backend
    ports:
      - "8086:8086"
#     volumes:
#       - ~/imagestorage:/application/images/products
#     networks:
#        int222:
#           ipv4_address: 172.10.0.3

# networks:
#   default:
#     external:
#        name: int222
#   int222:
#     external: true